<div class="container">
  <div class="row">
    <h2>Follows Users</h2>
    <% if @users.exists? %>
      <%= render 'relationships/relation', users: @users %>
        <% else %>
          <p>ユーザーはいません</p>
          <% end %>
  </div>
</div>